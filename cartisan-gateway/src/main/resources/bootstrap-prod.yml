spring:
  cloud:
    config:
      enabled: false
      discovery:
        enabled: true
        service-id: config
      profile: prod

eureka:
  client:
    service-url:
      defaultZone: http://www.hsqjp.com:8761/eureka

logging:
  path: /var/log/cartisan/gateway/
  file: cartisan-gateway
